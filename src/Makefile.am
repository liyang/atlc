# This is Makefile.am, which is proceed by automake
# @GUI_DIRECTORY@ will be set to 'gui' if the GUI needs to be built. 

SUBDIRS = non_gui @GUI_DIRECTORY@
EXTRA_DIST = README tests
DIST_SUBDIRS = non_gui  gui
